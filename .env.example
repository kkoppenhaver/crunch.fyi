# Anthropic API Key (required)
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# Redis connection URL (Railway provides this automatically when you add Redis)
REDIS_URL=redis://localhost:6379

# Server port (Railway sets this automatically)
PORT=3001

# Environment
NODE_ENV=production

# Max concurrent agent jobs (adjust based on resources)
MAX_CONCURRENT_JOBS=2

# Temp directory for cloned repos
TEMP_DIR=/tmp/crunch-repos

# Data directory for article storage (Railway volume mount point)
DATA_DIR=/data

# Langfuse observability (optional)
# Get yours at: https://langfuse.com/
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_BASE_URL=https://us.cloud.langfuse.com

# GitHub Personal Access Token (optional, for higher rate limits)
# Generate at: https://github.com/settings/tokens
# No scopes needed for public repos
GITHUB_TOKEN=
