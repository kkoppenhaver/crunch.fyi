# Anthropic API Key (required)
ANTHROPIC_API_KEY=sk-ant-...

# GitHub Personal Access Token (for API access - higher rate limits)
# Generate at: https://github.com/settings/tokens
# No scopes needed for public repos, 'repo' scope for private
GITHUB_TOKEN=ghp_...

# Redis connection URL
REDIS_URL=redis://localhost:6379

# Server port
PORT=3001

# Environment
NODE_ENV=development

# Max concurrent agent jobs (adjust based on VPS resources)
MAX_CONCURRENT_JOBS=2

# Temp directory for cloned repos (only used if USE_GIT_CLONE=true)
TEMP_DIR=/tmp/crunch-repos

# Analysis mode:
# - false (default): Use GitHub API to fetch repo context (faster, more secure)
# - true: Clone the full repo locally (legacy mode)
USE_GIT_CLONE=false
