# Anthropic API Key (required)
ANTHROPIC_API_KEY=sk-ant-...

# GitHub Personal Access Token (for API access - higher rate limits)
# Generate at: https://github.com/settings/tokens
# No scopes needed for public repos, 'repo' scope for private
GITHUB_TOKEN=ghp_...

# Redis connection URL
REDIS_URL=redis://localhost:6379

# Server port
PORT=3001

# Environment
NODE_ENV=development

# Max concurrent agent jobs (adjust based on VPS resources)
MAX_CONCURRENT_JOBS=2

# Temp directory for cloned repos (only used if ANALYSIS_MODE=clone)
TEMP_DIR=/tmp/crunch-repos

# Analysis mode:
# - 'scout' (default): Agent uses GitHub Scout tools to explore via API (no cloning)
# - 'clone': Legacy mode - clones the repo and uses file system tools
ANALYSIS_MODE=scout
